export type TUser = {
    id: string,
    nome: string,
    email: string,
    senha: string,
    criadoEm: string,
  };
  
  export type TProduct = {
    id: string,
    nome: string,
    preco: number,
    descricao: string,
    imageUrl: string,
  };